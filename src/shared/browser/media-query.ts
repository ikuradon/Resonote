// @public — Stable API for route/component/feature consumers
/**
 * Media query bridge — shared browser entry point for reactive matchMedia state.
 */

export { createMediaQuery } from './media-query.svelte.js';
