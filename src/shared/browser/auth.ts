// @public — Stable API for route/component/feature consumers
/**
 * Auth bridge — re-exports for app/feature access.
 */
export { getAuth, initAuth, loginNostr, logoutNostr } from './auth.svelte.js';
